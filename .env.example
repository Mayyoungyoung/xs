# CLI and Web app both read the key from the environment.
# Copy locally, replace the placeholder, and never commit the real key.
DEEPSEEK_API_KEY=replace_with_your_key
DEEPSEEK_MODEL=deepseek-v4-flash
